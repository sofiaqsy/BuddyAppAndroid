.gradle/
build/
local.properties
.idea/
*.iml
.DS_Store

# Firebase service account keys — nunca commitear
*firebase-adminsdk*.json
.kotlin/

# Config de Firebase: local, nunca al repo
google-services.json
app/google-services.json

# Llaves de firma: el repo es público, nunca deben viajar a GitHub.
*.jks
*.keystore

# Certificados exportados de las llaves de firma.
*.pem
