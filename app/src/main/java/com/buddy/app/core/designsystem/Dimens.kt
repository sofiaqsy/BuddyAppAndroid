package com.buddy.app.core.designsystem

import androidx.compose.ui.unit.dp

/** Espejo de `enum Spacing` (iOS). */
object Spacing {
    val xs   = 4.dp
    val sm   = 8.dp
    val md   = 16.dp
    val lg   = 24.dp
    val xl   = 36.dp
    val xxl  = 52.dp
    val edge = 20.dp
}

/** Espejo de `enum Radius` (iOS). */
object Radius {
    val sm = 14.dp
    val md = 16.dp
    val lg = 20.dp
    val xl = 24.dp
}
